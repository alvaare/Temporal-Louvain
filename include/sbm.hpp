#ifndef __SBM_HPP_INCLUDED__
#define __SBM_HPP_INCLUDED__

#include "graph.hpp"
#include "partition.hpp"

void tsbm(tempGraph&, int k, int n, int m, double p, double q);
//void tsbm(tempGraph*, partition&, int);

#endif